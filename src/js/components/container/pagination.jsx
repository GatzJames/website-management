var React = require('react');

var Pagination = React.createClass({

    render: function() {
        // isCurrentPage check if it is current page
        // pageNumber to show how many Pages
        //
        return (
            <ul className="pagination">

            </ul>
        );
    }

});

module.exports = Pagination;
